<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Club Rep Dashboard</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .club-dashboard { max-width: 1400px; margin: 20px auto; padding: 20px; background: white; border-radius: 12px; }
    .club-header { text-align: center; margin-bottom: 30px; }
    .club-header h1 { color: #333; font-size: 2.2em; margin-bottom: 5px; }
    .club-header .subtitle { color: #666; font-size: 1.1em; margin-bottom: 5px; }
    .club-header .rep-name { color: #667eea; font-weight: 600; font-size: 1em; }
    .logout-btn { display: inline-block; margin-top: 10px; padding: 10px 20px; background: #667eea; color: white; border-radius: 8px; border: none; cursor: pointer; font-weight: 600; }
    .logout-btn:hover { background: #5a6bd6; }
    
    .stats-container { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin-bottom: 30px; }
    .stat-card { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 20px; border-radius: 12px; text-align: center; }
    .stat-card h3 { font-size: 2.5em; margin: 0; }
    .stat-card p { margin: 5px 0 0 0; opacity: 0.9; }
    
    .tabs { display: flex; gap: 10px; margin-bottom: 20px; flex-wrap: wrap; }
    .tab-btn { padding: 10px 20px; background: #f0f0f0; border: 2px solid transparent; border-radius: 8px; cursor: pointer; font-weight: 600; transition: 0.3s ease; font-size: 0.9em; }
    .tab-btn.active { background: #667eea; color: white; }
    .tab-btn:hover { border-color: #667eea; }
    
    .tab-content { display: none; }
    .tab-content.active { display: block; }
    
    .form-section { background: #f8f9fa; padding: 25px; border-radius: 12px; margin-bottom: 20px; }
    .form-section h2 { color: #333; font-size: 1.5em; margin-bottom: 20px; border-bottom: 2px solid #667eea; padding-bottom: 10px; }
    
    .form-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 15px; margin-bottom: 15px; }
    .form-group label { display: block; color: #333; font-weight: 600; margin-bottom: 5px; }
    .form-group input, .form-group select, .form-group textarea { width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 6px; font-size: 1em; font-family: inherit; }
    .form-group input:focus, .form-group select:focus, .form-group textarea:focus { outline: none; border-color: #667eea; box-shadow: 0 0 5px rgba(102,126,234,0.3); }
    .form-group textarea { resize: vertical; min-height: 80px; }
    .form-group input[type="checkbox"] { width: auto; margin-right: 8px; }
    .checkbox-label { display: flex; align-items: center; font-weight: 400; }
    
    .btn-submit { padding: 10px 25px; background: #667eea; color: white; border: none; border-radius: 6px; cursor: pointer; font-weight: 600; transition: 0.3s ease; }
    .btn-submit:hover { background: #5a6bd6; }
    
    .items-list { margin-top: 20px; }
    .item-card { background: white; padding: 15px; border-radius: 8px; margin-bottom: 10px; border-left: 4px solid #667eea; display: flex; justify-content: space-between; align-items: flex-start; }
    .item-info h4 { color: #333; margin: 0 0 5px 0; font-size: 1.1em; }
    .item-info p { color: #666; margin: 2px 0; font-size: 0.9em; }
    .item-meta { color: #999; font-size: 0.85em; margin-top: 5px; }
    
    .event-card { border-left-color: #17a2b8; }
    .member-card { border-left-color: #28a745; }
    .announcement-card { border-left-color: #ffc107; }
    .announcement-card.pinned { background: #fffbea; }
    
    .status-badge { display: inline-block; padding: 4px 10px; border-radius: 4px; font-size: 0.8em; font-weight: 600; }
    .status-upcoming { background: #d1ecf1; color: #0c5460; }
    .status-ongoing { background: #fff3cd; color: #856404; }
    .status-completed { background: #d4edda; color: #155724; }
    .status-cancelled { background: #f8d7da; color: #721c24; }
    
    .role-badge { display: inline-block; padding: 4px 10px; border-radius: 4px; font-size: 0.8em; font-weight: 600; background: #e7e7ff; color: #667eea; }
    
    .btn-delete { padding: 6px 14px; background: #dc3545; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 0.85em; margin-right: 5px; }
    .btn-delete:hover { background: #c82333; }
    
    .btn-status { padding: 6px 14px; background: #17a2b8; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 0.85em; }
    .btn-status:hover { background: #138496; }
    
    .message { padding: 12px 15px; border-radius: 6px; margin-bottom: 15px; }
    .message.success { background: #d4edda; color: #155724; border: 1px solid #c3e6cb; }
    .message.error { background: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; }
    
    .action-buttons { display: flex; gap: 5px; flex-wrap: wrap; }
    
    @media (max-width: 768px) {
      .form-row { grid-template-columns: 1fr; }
      .item-card { flex-direction: column; }
      .stat-card h3 { font-size: 2em; }
      .tabs { gap: 5px; }
      .tab-btn { padding: 8px 12px; font-size: 0.8em; }
    }
  </style>
</head>
<body>
  <div class="club-dashboard">
    <div class="club-header">
      <h1>🎯 <%= clubName %></h1>
      <p class="subtitle">Club Dashboard</p>
      <p class="rep-name">Managed by: <%= clubRepName %></p>
      <form action="/club-rep/logout" method="POST" style="display: inline;">
        <button type="submit" class="logout-btn">Logout</button>
      </form>
    </div>

    <% if (successMessage) { %>
      <div class="message success"><%= successMessage %></div>
    <% } %>
    <% if (errorMessage) { %>
      <div class="message error"><%= errorMessage %></div>
    <% } %>

    <!-- Stats Section -->
    <div class="stats-container">
      <div class="stat-card">
        <h3><%= totalEvents %></h3>
        <p>Events Organized</p>
      </div>
      <div class="stat-card">
        <h3><%= totalMembers %></h3>
        <p>Club Members</p>
      </div>
      <div class="stat-card">
        <h3><%= totalAnnouncements %></h3>
        <p>Announcements</p>
      </div>
    </div>

    <!-- Tabs Navigation -->
    <div class="tabs">
      <button class="tab-btn active" onclick="switchTab('add-event')">Create Event</button>
      <button class="tab-btn" onclick="switchTab('add-member')">Add Member</button>
      <button class="tab-btn" onclick="switchTab('add-announcement')">Post Announcement</button>
      <button class="tab-btn" onclick="switchTab('view-events')">View Events</button>
      <button class="tab-btn" onclick="switchTab('view-members')">View Members</button>
      <button class="tab-btn" onclick="switchTab('view-announcements')">View Announcements</button>
    </div>

    <!-- Create Event Tab -->
    <div id="add-event" class="tab-content active">
      <div class="form-section">
        <h2>Create New Event</h2>
        <form action="/club-rep/dashboard/add-event" method="POST">
          <div class="form-row">
            <div class="form-group">
              <label>Event Name</label>
              <input type="text" name="eventName" placeholder="e.g., Tech Workshop" required>
            </div>
            <div class="form-group">
              <label>Event Type</label>
              <select name="eventType">
                <option value="workshop">Workshop</option>
                <option value="meetup">Meetup</option>
                <option value="competition">Competition</option>
                <option value="social">Social Event</option>
                <option value="webinar">Webinar</option>
                <option value="other">Other</option>
              </select>
            </div>
          </div>
          <div class="form-row">
            <div class="form-group">
              <label>Description</label>
              <textarea name="description" placeholder="Event details and description" required></textarea>
            </div>
          </div>
          <div class="form-row">
            <div class="form-group">
              <label>Event Date</label>
              <input type="date" name="eventDate" required>
            </div>
            <div class="form-group">
              <label>Event Time</label>
              <input type="text" name="eventTime" placeholder="e.g., 2:00 PM - 4:00 PM" required>
            </div>
          </div>
          <div class="form-row">
            <div class="form-group">
              <label>Location/Venue</label>
              <input type="text" name="location" placeholder="e.g., Main Auditorium" required>
            </div>
            <div class="form-group">
              <label>Capacity (Optional)</label>
              <input type="number" name="capacity" placeholder="Max participants">
            </div>
          </div>
          <button type="submit" class="btn-submit">Create Event</button>
        </form>
      </div>
    </div>

    <!-- Add Member Tab -->
    <div id="add-member" class="tab-content">
      <div class="form-section">
        <h2>Add Club Member</h2>
        <form action="/club-rep/dashboard/add-member" method="POST">
          <div class="form-row">
            <div class="form-group">
              <label>Member Name</label>
              <input type="text" name="name" placeholder="Full name" required>
            </div>
            <div class="form-group">
              <label>Email</label>
              <input type="email" name="email" placeholder="student@college.edu" required>
            </div>
          </div>
          <div class="form-row">
            <div class="form-group">
              <label>Phone Number</label>
              <input type="text" name="phoneNumber" placeholder="Phone number">
            </div>
            <div class="form-group">
              <label>Roll Number</label>
              <input type="text" name="rollNumber" placeholder="e.g., CS101">
            </div>
          </div>
          <div class="form-row">
            <div class="form-group">
              <label>Department</label>
              <input type="text" name="department" placeholder="e.g., CSE">
            </div>
            <div class="form-group">
              <label>Role in Club</label>
              <select name="role">
                <option value="member">Member</option>
                <option value="secretary">Secretary</option>
                <option value="treasurer">Treasurer</option>
                <option value="vice-president">Vice President</option>
                <option value="president">President</option>
              </select>
            </div>
          </div>
          <button type="submit" class="btn-submit">Add Member</button>
        </form>
      </div>
    </div>

    <!-- Post Announcement Tab -->
    <div id="add-announcement" class="tab-content">
      <div class="form-section">
        <h2>Post Announcement</h2>
        <form action="/club-rep/dashboard/add-announcement" method="POST">
          <div class="form-row">
            <div class="form-group">
              <label>Title</label>
              <input type="text" name="title" placeholder="Announcement title" required>
            </div>
            <div class="form-group">
              <label>Category</label>
              <select name="category">
                <option value="update">Update</option>
                <option value="recruitment">Recruitment</option>
                <option value="achievement">Achievement</option>
                <option value="highlight">Highlight</option>
                <option value="other">Other</option>
              </select>
            </div>
          </div>
          <div class="form-row">
            <div class="form-group">
              <label>Content</label>
              <textarea name="content" placeholder="Announcement content" required></textarea>
            </div>
          </div>
          <div class="form-row">
            <div class="form-group" style="flex-direction: row; align-items: center;">
              <input type="checkbox" name="isPinned" id="isPinned">
              <label for="isPinned" class="checkbox-label">Pin this announcement (Important)</label>
            </div>
          </div>
          <button type="submit" class="btn-submit">Post Announcement</button>
        </form>
      </div>
    </div>

    <!-- View Events Tab -->
    <div id="view-events" class="tab-content">
      <div class="form-section">
        <h2>Events (<%= events.length %>)</h2>
        <div class="items-list">
          <% if (events && events.length > 0) { %>
            <% events.forEach(event => { %>
              <div class="item-card event-card">
                <div class="item-info">
                  <h4><%= event.eventName %> <span class="status-badge status-<%= event.status %>"><%= event.status %></span></h4>
                  <p><strong>Type:</strong> <%= event.eventType %> | <strong>Capacity:</strong> <%= event.capacity || 'Unlimited' %></p>
                  <p><strong>Description:</strong> <%= event.description.substring(0, 80) %>...</p>
                  <p><strong>Date:</strong> <%= new Date(event.eventDate).toLocaleDateString() %> at <%= event.eventTime %></p>
                  <p><strong>Location:</strong> <%= event.location %></p>
                  <div class="item-meta">
                    <p><strong>Created:</strong> <%= new Date(event.createdAt).toLocaleDateString() %></p>
                  </div>
                </div>
                <div class="action-buttons">
                  <% if (event.status !== 'completed' && event.status !== 'cancelled') { %>
                    <form action="/club-rep/dashboard/update-event-status" method="POST" style="display: inline;">
                      <input type="hidden" name="eventId" value="<%= event._id %>">
                      <input type="hidden" name="status" value="<%= event.status === 'upcoming' ? 'ongoing' : 'completed' %>">
                      <button type="submit" class="btn-status"><%= event.status === 'upcoming' ? 'Start Event' : 'Mark Complete' %></button>
                    </form>
                  <% } %>
                  <form action="/club-rep/dashboard/delete-event" method="POST" style="display: inline;" onsubmit="return confirm('Delete this event?');">
                    <input type="hidden" name="eventId" value="<%= event._id %>">
                    <button type="submit" class="btn-delete">Delete</button>
                  </form>
                </div>
              </div>
            <% }); %>
          <% } else { %>
            <p style="color: #999; text-align: center; padding: 20px;">No events created yet</p>
          <% } %>
        </div>
      </div>
    </div>

    <!-- View Members Tab -->
    <div id="view-members" class="tab-content">
      <div class="form-section">
        <h2>Club Members (<%= members.length %>)</h2>
        <div class="items-list">
          <% if (members && members.length > 0) { %>
            <% members.forEach(member => { %>
              <div class="item-card member-card">
                <div class="item-info">
                  <h4><%= member.name %> <span class="role-badge"><%= member.role %></span></h4>
                  <p><strong>Email:</strong> <%= member.email %></p>
                  <p><strong>Roll:</strong> <%= member.rollNumber || 'N/A' %> | <strong>Dept:</strong> <%= member.department || 'N/A' %></p>
                  <p><strong>Phone:</strong> <%= member.phoneNumber || 'N/A' %></p>
                  <p><strong>Status:</strong> <%= member.status %></p>
                  <div class="item-meta">
                    <p><strong>Joined:</strong> <%= new Date(member.joinDate).toLocaleDateString() %></p>
                  </div>
                </div>
                <form action="/club-rep/dashboard/delete-member" method="POST" style="display: inline;" onsubmit="return confirm('Remove this member?');">
                  <input type="hidden" name="memberId" value="<%= member._id %>">
                  <button type="submit" class="btn-delete">Remove</button>
                </form>
              </div>
            <% }); %>
          <% } else { %>
            <p style="color: #999; text-align: center; padding: 20px;">No members added yet</p>
          <% } %>
        </div>
      </div>
    </div>

    <!-- View Announcements Tab -->
    <div id="view-announcements" class="tab-content">
      <div class="form-section">
        <h2>Announcements (<%= announcements.length %>)</h2>
        <div class="items-list">
          <% if (announcements && announcements.length > 0) { %>
            <% announcements.forEach(announcement => { %>
              <div class="item-card announcement-card <%= announcement.isPinned ? 'pinned' : '' %>">
                <div class="item-info">
                  <h4><%= announcement.title %> <% if (announcement.isPinned) { %>📌<% } %></h4>
                  <p><strong>Category:</strong> <%= announcement.category %></p>
                  <p><strong>Content:</strong> <%= announcement.content.substring(0, 120) %>...</p>
                  <div class="item-meta">
                    <p><strong>Posted:</strong> <%= new Date(announcement.createdAt).toLocaleDateString() %></p>
                  </div>
                </div>
                <form action="/club-rep/dashboard/delete-announcement" method="POST" style="display: inline;" onsubmit="return confirm('Delete this announcement?');">
                  <input type="hidden" name="announcementId" value="<%= announcement._id %>">
                  <button type="submit" class="btn-delete">Delete</button>
                </form>
              </div>
            <% }); %>
          <% } else { %>
            <p style="color: #999; text-align: center; padding: 20px;">No announcements posted yet</p>
          <% } %>
        </div>
      </div>
    </div>
  </div>

  <script>
    function switchTab(tabName) {
      // Hide all tabs
      const tabs = document.querySelectorAll('.tab-content');
      tabs.forEach(tab => tab.classList.remove('active'));
      
      // Remove active class from all buttons
      const buttons = document.querySelectorAll('.tab-btn');
      buttons.forEach(btn => btn.classList.remove('active'));
      
      // Show selected tab
      document.getElementById(tabName).classList.add('active');
      
      // Add active class to clicked button
      event.target.classList.add('active');
    }
  </script>
</body>
</html>
